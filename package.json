{
    "name": "got",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*",
        "packages/pulumi-gotiac-aws/sdk/*"
    ],
    "devDependencies": {
        "prettier": "^3.2.5"
    },
    "dependencies": {
        "zod": "^3.23.3"
    },
    "scripts": {
        "pretty": "prettier . --write",
        "update:deps": "bun x npm-check-updates -ui -ws --peer"
    }
}
